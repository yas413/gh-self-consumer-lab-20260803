name: GH-R60 caller nested inner collision
description: Caller collision; must not resolve

outputs:
  marker:
    description: Caller collision marker
    value: ${{ steps.marker.outputs.value }}

runs:
  using: composite
  steps:
    - id: marker
      shell: bash
      run: printf '%s\n' 'value=CALLER_NESTED_INNER_COLLISION' >> "$GITHUB_OUTPUT"
